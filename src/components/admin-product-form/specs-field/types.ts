export type AdminSpecsFieldProps = {
  name: string;
  title: string;
  helperText: string;
  initialSpecs: Array<{ label: string; value: string }>;
  labelTitle: string;
  valueTitle: string;
  addButtonLabel: string;
  removeButtonLabel: string;
};